# Terraform state files (contain sensitive data)
*.tfstate
*.tfstate.*

# Terraform crash logs
crash.log

# Terraform provider plugins
.terraform/
.terraform.lock.hcl

# Terraform plan files
*.tfplan

# Ignore override files (local settings)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore variables files (if they contain secrets)
terraform.tfvars
terraform.tfvars.json
